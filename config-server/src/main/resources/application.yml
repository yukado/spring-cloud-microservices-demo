server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yukado/microservices-config-repo
          default-label: main
          clone-on-start: true
